# What changes from LDSC2

We want to emphasize that there is a lot of interface changes and you should not build the commands by translating from LDSC2 commands. It is very likely that you could misspecify the LDSC3 arguments.



For sanity check only.

separate baseline and query annotations in input

preprocessing (annotating) step is integrated into `ldscore`

Defaults to chr::pos rather than rsid

deprecate the baseline annotations






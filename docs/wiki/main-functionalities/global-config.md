# global config



# spec path rules

glob pattern





-file is one file

-sources can be globbed

give examples



# general output directory format

`diagnostics/` contain logs and provenance metadata. The directory is not consumed by downstream modules.

explain more.
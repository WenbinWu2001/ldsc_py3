# C1–C3 cleanup disposition

Last updated on: 2026-09-10

Historical inventory: [2026-09-08 architecture review](../audits/2026-09-08-architecture-review.html), candidates C1–C3. The dated report and all candidate IDs are unchanged. Starting HEAD: `eec653a`; branch: `restructure`. The pre-existing partitioned-h2 wiki edit and untracked report are preserved. No push or remote work.

Execution order: establish C3 verification, commit private dead-code cleanup separately, resolve C1 interface/computation decisions, then consolidate C2 documentation. Every finding below is resolved, already resolved, or explicitly retained. No interface decision is awaiting user input.

## Complete disposition

| ID | Finding | Disposition and evidence |
| --- | --- | --- |
| C3.1 | Missing golden regeneration | Resolved: build golden is checked before computation; missing-file regression proves no writes. Reader golden is loaded before reader construction. No NPZ was regenerated. |
| C3.2 | 26 per-test path bootstraps, sitecustomize, tests init, pytest pythonpath | Resolved: editable installation is the supported policy for pytest, unittest and direct test invocation. HM3 tool tests load the standalone script with runpy. |
| C3.3 | Packaging test forced source imports | Resolved: isolated outside-repository import, console/module CLI and resource tests; a built wheel also passes in a temporary installation. |
| C3.4 | Five unused fixtures and useful marker hook | Resolved: removed fixtures_root, annotation_fixtures, format_fixtures, plink_fixtures and minimal_external_resources after consumer/dynamic-request search. Collection-marker hook remains. |
| C3.5 | 11 orphan format remnants and README | Resolved: all 11 listed files retired; fixture README describes current format coverage and immutable goldens. Complete legacy suites generated by converter tests remain. |
| C3.6 | Golden, generator, independent numerical and real PLINK coverage | Retained deliberately: both golden NPZs, gen_reader_golden.py, real PLINK subsets, independent dense/slow oracles and legitimate legacy formats. Resource bytes match starting HEAD. |
| C1.1 | Obsolete annotation/LD-score helpers | Resolved: removed _make_single_annotation_file, chromosome_set_from_annotation_inputs, resolve_annotation_files, the resulting orphan split_arg_list, and their unused imports. Active writers and format support remain. |
| C1.2 | Runtime state and allocation | Resolved: removed _runtime_layout and only the unused reference_ids frozenset in the chromosome wrapper. Other reference sets remain active. |
| C1.3 | Duplicate chromosome result | Resolved: removed _LegacyChromResult; wrapper annotation and tests use kernel ChromComputationResult, including its current identity_drops field. |
| C1.4 | Self-module lookup | Already resolved by A1 (4082e04): prepared-state primitives replace get_legacy_ld_module; active bitarray checks remain. |
| C1.5 | Parallel atom statistics and annotation reconstruction | Resolved: production indexed assembly uses assemble_selected_atom_statistics. Test-only runtime scattering removed; independent SNP-grid reconstruction and hand-worked counts/overlaps remain in tests. |
| C1.6 | Former index state/lock migration | Retained deliberately: dual locks protect a destination against earlier writers; recognized former logs are archived without losing history. There is no evidence that earlier writers are absent. Active flock, stable build-state, complete publication and recovery remain unchanged; existing concurrency/publication tests pass. |
| C1.7 | Retired dataframe writer | Resolved: removed _maybe_write_dataframe; collision assertions now exercise real H2/PartitionedH2/Rg writer artifact families. |
| C1.8 | Ancient pandas probe/commented import | Already resolved by A4 cleanup (84fc522); current dependency constraints remain. |
| C1.9 | 12 unused munging workflow exports | Already resolved by 84fc522 before A4 (8e513b9). Removed names are enumerated below; active workflow helpers remain. |
| C1.10 | Forwarding spelling aliases | Resolved: infer_chr_bp_columns and getBlockLefts removed; callers use infer_chr_pos_columns and get_block_lefts. Accepted column aliases and legacy input formats remain unchanged. |
| C1.11 | Ignored validate/summary arguments | Resolved deliberately: AnnotationBundle.validate() has no identifier argument; SumstatsMunger.build_run_summary() has no table argument. Validation and last-run accounting remain; callers/tutorial updated. |
| C1.12 | fail_on_missing_metadata | Resolved deliberately: inert public setting, propagation and snapshot field removed. Required provenance validation remains unconditional; loaders recover identity fields explicitly, tolerating unrelated old snapshot keys. |
| C1.13 | Deprecated CLI controls and Python alias | Resolved deliberately: --write-per-query-results is rejected; include_model_categories is removed. Active include_full_partitioned_h2 and PartitionedH2OutputConfig.write_per_query_results remain covered. |
| C1.14 | Repeated declared-file and atomic-JSON helpers | Resolved: _result_files shares narrow path containment and JSON publication for plotting/h2_scale. Workflow-specific metadata/table validation stays local. outputs retains its different NumPy/dataclass serializer. |
| C1.15 | Routine output-directory warning | Resolved deliberately: explicit directory creation logs INFO; collisions still fail and scientific warnings remain. No-write inference bypass remains intact. |
| C1.16 | Legacy estimator text summaries | Retained deliberately: estimate_h2/estimate_rg return estimators to Python users. Hsq.summary, Gencov.summary and RG.summary remain reachable public behavior; absence of internal callers does not justify removal. overlap_matrix still uses _overlap_output. |
| C2.1 | Competing navigation and 28 obsolete design-map paths | Resolved: code-structure.md is the authoritative contributor entry. Useful domain ownership and 23 distinct historical decision links consolidated; old map archived with an explicit historical label. |
| C2.2 | Six obsolete current-doc path occurrences | Resolved: all docs/superpowers paths in docs/current now resolve to existing docs/specs or docs/plans files. |
| C2.3 | query-r2 output and region ownership | Resolved: current entry cites required output-dir/QueryR2DirectoryWriter and regression-only masks; removed RefPanel exclusion methods/builder fields survive only in the clearly archived map. |
| C2.4 | Footer versus sidecar statements | Resolved: sumstats provenance comes from the Parquet footer; footerless Parquet fails; legacy text remains supported. R2 sidecars and their MAF are mandatory. Corrected config docs, inference docs and affected tutorial. |
| C2.5 | CM/MAF/traversal statements | Original source comments already resolved by A1 (4082e04). Removed stale merge_frequency_metadata reference and clarified required sidecar MAF. Reference CM/MAF are authoritative, annotation CM writes NA, and parquet accumulation streams pairs. |
| C2.6 | Completed root threshold audit files | Resolved: task_plan.md, progress.md and findings.md moved together to docs/audits/threshold-comparison with historical context, current boundary links and HM3 builder relocation. Original conclusions retained as historical evidence. |
| C2.7 | Links, inline paths/symbols and README/tutorial examples | Resolved: local Markdown files/anchors and inline concrete paths checked; current docs scanned against source functions removed since audit. Cell-specific notebook repaired for current overlap contract and installed imports, executed through real Python/CLI paths. Dated specs, audit, scientific rationale, equivalence evidence and lessons preserved. |

## All 15 reported import bindings

Current AST import inspection confirms all 15 are absent. Deliberate reexports such as `PlinkBEDFile` and the tested `identifiers.clean_header` remain.

| ID | Source under src/ldsc | Binding | Disposition |
| --- | --- | --- | --- |
| C1.I1 | `legacy_ldscore_converter.py` | `Any` | Already removed: c00b07d |
| C1.I2 | `ref_panel_builder.py` | `split_cli_path_tokens` | Removed: 50801f7 |
| C1.I3 | `regression_runner.py` | `REGENERATE_ARTIFACT_MESSAGE` | Already removed: 49b2677 |
| C1.I4 | `regression_runner.py` | `LDSCConfigError` | Already removed: 49b2677 |
| C1.I5 | `ldscore_calculator.py` | `ConfigMismatchError` | Already removed: fa2f004 |
| C1.I6 | `gene_list_resolver.py` | `StringIO` | Removed: 50801f7 |
| C1.I7 | `sumstats_munger.py` | `LDSCInternalError` | Already removed: 84fc522 |
| C1.I8 | `_kernel/ldscore.py` | `OrderedDict` | Already removed: fa2f004 |
| C1.I9 | `_kernel/ldscore.py` | `MAF_COLUMN_ALIASES` | Already removed: fa2f004 |
| C1.I10 | `_kernel/ldscore.py` | `RESTRICTION_CHRPOS_SPEC_MAP` | Already removed: fa2f004 |
| C1.I11 | `_kernel/ldscore.py` | `RESTRICTION_RSID_SPEC_MAP` | Already removed: fa2f004 |
| C1.I12 | `_kernel/ref_panel.py` | `LDSCInternalError` | Already removed: fa2f004 |
| C1.I13 | `_kernel/ref_panel.py` | `resolve_scalar_path` | Already removed: fa2f004 |
| C1.I14 | `_kernel/ref_panel.py` | `REGENERATE_ARTIFACT_MESSAGE` | Already removed: fa2f004 |
| C1.I15 | `_kernel/identifiers.py` | `coordinate_missing_mask` | Removed: 50801f7 |

## Enumerated retirements

The 12 munging exports were `describe_cname`, `numeric_cols`, `filter_pvals`, `filter_info`, `filter_frq`, `filter_alleles`, `parse_dat`, `process_n`, `p_to_z`, `check_median`, `parse_flag_cnames`, and `munge_sumstats`.

The 11 retired files under `tests/fixtures/formats/` were `test.cts`, `test.l2.M`, `test.l2.ldscore.gz`, `test.sumstats`, `test1.frq`, `test1.l2.M`, `test1.l2.ldscore.bz2`, `test2.frq.gz`, `test2.l2.M`, `test2.l2.ldscore.bz2`, and `test_bad.l2.M`.

## Verification

Development interpreter: `/Users/wenbinwu/miniforge3/envs/ldsc3-dev/bin/python` (Python 3.13). Tests run against the existing editable installation. Pytest and unittest ran sequentially.

| Check | Actual result |
| --- | --- |
| C3 full pytest | 1,390 passed, 1 skipped, 128 subtests; 77.59 s |
| C3 unittest | 1,000 tests, OK, 1 skipped; 49.905 s |
| Private cleanup focused tests | 434 passed, 71 subtests |
| Inert-control/writer/A8 checks | 435 passed, 46 subtests |
| Atom statistics, index lifecycle, plotting and scale checks | 93 passed |
| Derived lifecycle and invalid declared paths | 11 passed, including both workflows rejecting absolute/parent/symlink escapes and missing files without writes |
| Final full pytest | 1,399 passed, 1 skipped, 143 warnings, 128 subtests; 78.88 s |
| Final unittest | 1,001 tests, OK, 1 skipped; 49.537 s |
| Real inference-only CLI | 16 subprocess cases: console/module entry points, valid/missing input, absent/existing output, with/without overwrite; all preserve file bytes and mtimes, directory absence and existing RUN_FAILED marker |
| Real affected CLI help | ldscore, build-ref-panel, query-r2, partitioned-h2, munge-sumstats, plot, convert-h2-scale: passed; retired flag rejection creates no output |
| Built-wheel smoke | Built without dependency downloads, installed in a temporary environment; isolated import, new helper inclusion, retired token-parser absence, console/module help, packaged HM3/region resources, lazy plotting and pip check passed |
| Cell-specific tutorial notebook | All 5 code cells executed; two-query manifest and API/CLI coefficient/SE parity passed within float32 artifact precision (rtol 1e-6, atol 1e-12); outputs remain uncommitted |
| Documentation | 151 local Markdown targets/anchors and 134 concrete inline path references checked; current docs scanned against removed source functions; no unresolved current references |

The missing-golden test was observed failing before implementation using a temporary missing path; committed NPZs were untouched. The CLI-rejection and normal-directory INFO tests were also observed red before their changes. Scientific golden/numerical tolerances were not changed. Notebook comparison accounts for the existing `outputs._cast_parquet_floats` serialization boundary.

## Commits and retained boundaries

- `6c96f10` — protect immutable goldens and use installed test imports.
- `50801f7` — remove verified private remnants before structural changes.
- `8632d5e` — retire inert public controls and update callers/contracts.
- `f26b0e8` — use production atom sufficient statistics; retain independent oracles.
- `d095cd7` — share narrow derived-result file operations.
- `2b2e3dc` — remove the token parser orphaned by the obsolete annotation resolver (207 focused tests passed).
- `257a626` — consolidate contributor navigation, archive prior maps/audit notes, repair current references and execute the affected notebook.

The preceding A1 (`b9a6ea0`, `fa2f004`, `4082e04`), A2 (`49b2677`, `c00b07d`, `948e35d`), A3 (`de6267e`), A8 (`bcaf1e0`), and A4 (`84fc522`, `8e513b9`) fixes and relevant diffs were reviewed against current code. Later A5/A6 commits (`35f953f`, `eec653a`) were accounted for when inspecting current ownership.

Retained decisions are substantive boundaries, not unfinished deletions: cross-version lock/log protection, public estimator text summaries, differing output JSON serialization, active Python output controls, scientific oracles and legitimate legacy formats. No new migration framework or rollback was introduced. Numerical artifact schemas and scientific conventions are preserved; retirement of the inert config snapshot field is documented explicitly.

Verification commands were `python -m pytest -q` and `python -m unittest discover -s tests -p 'test*.py' -v` from the repository root, using the interpreter above. The sole skip is the missing-PyArrow dependency case because PyArrow is installed. Logs for this session are `/tmp/c1-c3-pytest-delivered.log`, `/tmp/c1-c3-unittest-delivered.log`, `/tmp/c1-c3-wheel-delivered.log`, `/tmp/c1-c3-cli-final.log`, and `/tmp/c1-c3-notebook.log`. Wheel checks used temporary installations with existing dependencies, not a Python 3.11–3.13 installation matrix. No HPC or large-scale performance benchmark was run. Task-created repository build files were removed after validation.
